---
description: 에러·장애 디버깅 후 런북을 스킬로 기록
agent: fixy
---
다음 에러/장애를 진단하고 고친다:

$ARGUMENTS

진행 순서:
1. `fixy_recall` 로 같은/비슷한 에러를 전에 디버깅했는지 확인한다(런북 재사용).
2. 재현 → 근본 원인 격리 → 최소 수정. 추측 금지, 증거(로그·스택·코드) 기반.
3. 수정 후 재현 경로가 사라졌는지 검증한다.
4. **재발 가능성이 있으면** 디버깅 런북을 `fixy_note`(insight) 로 남긴다: 증상 → 원인 → 해결 → 예방. 같은 류가 쌓이면 `/fixy-evolve` 가 스킬로 승급한다.

증상·근본원인·수정·검증·기록을 한국어로 간결히 보고한다.
